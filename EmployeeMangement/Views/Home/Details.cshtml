@model HomeDetailsViewModel;
@{
    var photophath = "~/Image/" + (Model.Employee.PhotoPath ?? "3164c1f0-cba6-4c85-a34d-cadd1854e618_asdjpg.jpg");
}
<html>
<head>
    <title></title>
</head>
<body>
    @*use view bag viewdata*@
    @*<h1>@ViewData["PageTittle"]</h1>
        <h1>@ViewBag.PageTittle</h1>
        @{
            var modelViewBag = ViewBag.Employee;
            var modelViewData = ViewData["Employee"] as EmployeeMangement.Models.Employee;//casting viewdata string to employe class
        }
        <h4>@modelViewBag.Name</h4>
        <h4>@modelViewBag.Id</h4>
        <h4>@modelViewData.Department</h4>
        <h4>@modelViewData.Email</h4>
        <h4>@Model.PageTitle</h4>
        <h4>@Model.Employee.Name</h4>*@

    <div class="card m-3">
        <div class="card-header">
            <h3>@Model.Employee.Name</h3>

        </div>
        <div class="card-body text-center" >
            <img class="card-img-top" src=@photophath  asp-append-version="true"/>
            <h3>@Model.Employee.Department</h3>
            <h3>@Model.Employee.Email</h3>
        </div>
        <div class=" card-footer text-center">
            <a class="btn btn-primary" asp-controller="Home" asp-action="Edit" asp-route-id="@Model.Employee.Id">edit</a>
            <a class="btn btn-danger" asp-controller="Home" asp-action="Delete" asp-route-id="@Model.Employee.Id">Delete</a>
        </div>
    </div>
    @section Scripts
{
        <script src="~/js/custem.js"></script>
    }
</body>

</html>